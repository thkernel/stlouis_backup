$("ul.errors").html("");
<% if @food.errors.any? %>
  <% @food.errors.full_messages.each do |message| %>
 
    $("ul.errors").append($('<div class="c-alert c-alert--danger" > ').html("<%= message.html_safe %> "));
  <% end %>
<% else %>
  $(".foods").html("<%= escape_javascript(render 'index') %>");
  $("#food-modal").modal("hide");
 
<% end %>