$("ul.errors").html("");
<% if @fidelity_card.errors.any? %>
  <% @fidelity_card.errors.full_messages.each do |message| %>
 
    $("ul.errors").append($('<div class="c-alert c-alert--danger" > ').html("<%= message.html_safe %> "));
  <% end %>
<% else %>
  $(".fidelity-cards").html("<%= escape_javascript(render 'index') %>");
  $("#fidelity-card-modal").modal("hide");
 
<% end %>